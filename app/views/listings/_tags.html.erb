<%# locals: (listing:) -%> 

<%= tag.layout_container class: "field is-grouped is-grouped-multiline" do %>
  <% listing.tags.each do |value| %> 
    <%= render "tag", value: %> 
  <% end %>
<% end %>
